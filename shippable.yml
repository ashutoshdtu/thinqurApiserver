language: java                             

jdk:                                      
  - openjdk7                  

env:                                                
  - #env1=foo                           
  - #env2=bar

matrix:                                                                 

build:                                                  

  pre_ci: 
    - ./activator clean

  ci:
    - ./activator test
    - ./activator docker:stage
  
  push:
    - ./activator docker:publish

integrations:
  hub:
    - integrationName: my-docker-integration
      type: docker
      branches:
        only:
          - master
