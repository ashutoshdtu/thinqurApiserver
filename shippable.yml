language: java                             

jdk:                                      
  - openjdk7                  

matrix:                                                                 

build:                                                  

  pre_ci: 
    - ./activator clean

  ci:
    - ./activator test
    - ./activator docker:stage
  
  push:
    - ./activator docker:publish

integrations:
  hub:
    - integrationName: my-docker-integration
      type: docker
      branches:
        only:
          - master
